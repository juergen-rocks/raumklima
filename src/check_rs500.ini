[redis]
host = raumklima
port = 6379
db = 3
prefix = rs500_
# Socket & Connect timeout.
# Remember: if you run within a 30s cronjob
#           you should end the thread within
#           this interval
socket_timeout=10
